quick-save:
	git add --all
	git commit -m "Save"
	git push

libft:
	git checkout libft

ft_printf:
	git checkout ft_printf

get_next_line:
	git checkout get_next_line

so_long:
	git checkout so_long

push_swap:
	git checkout push_swap

minitalk:
	git checkout minitalk

philo:
	git checkout philosophers

minishell:
	git checkout minishell